class Movie(object):

    def __init__(self, title_in_source, title_in_platform, url):
        self.title_in_source = title_in_source
        self.title_in_platform = title_in_platform
        self.url = url
